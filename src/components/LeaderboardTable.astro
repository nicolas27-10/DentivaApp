---
// Importa tu cliente de Supabase (ajusta la ruta según tu proyecto)
// import { supabase } from '@/lib/supabaseClient';

// // 1. Pedimos a Supabase los campos username y total_fsp_points
// // 2. Los ordenamos de mayor a menor (ascending: false)
// // 3. Limitamos a los mejores 10
// const { data: leaderboard, error } = await supabase
//   .from('profiles')
//   .select('username, total_fsp_points')
//   .order('total_fsp_points', { ascending: false })
//   .limit(10);

// if (error) {
//   console.error("🛑 Error cargando la leaderboard:", error);
// }

// // Opcional: Si quieres asegurarte de que no traiga usuarios con 0 puntos
// const activeUsers = leaderboard?.filter(user => user.total_fsp_points > 0) || [];
---



<div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-8 flex flex-col items-center justify-center text-center relative overflow-hidden">
  
  <!-- Efecto visual de "Próximamente" -->
  <div class="absolute inset-0 bg-gray-50/50 backdrop-blur-[2px] flex items-center justify-center z-10">
    <div class="bg-white px-6 py-3 rounded-full border border-gray-200 shadow-sm flex items-center gap-3">
      <span class="relative flex h-3 w-3">
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-primary opacity-75"></span>
        <span class="relative inline-flex rounded-full h-3 w-3 bg-primary"></span>
      </span>
      <span class="text-sm font-bold text-textMain uppercase tracking-widest">
        Bald verfügbar
      </span>
    </div>
  </div>

  <!-- Estructura visual atenuada (simulando cómo se verá) -->
  <div class="w-full opacity-30 pointer-events-none">
    <div class="flex items-center justify-between p-3 rounded-xl bg-gray-50 mb-2">
      <div class="flex items-center gap-4">
        <span class="font-black text-lg w-6 text-yellow-500">1</span>
        <div class="w-8 h-8 rounded-full bg-blue-100"></div>
        <span class="font-semibold text-gray-700">Max Mustermann</span>
      </div>
      <div class="font-bold text-[#2B8EB5] bg-blue-50 px-3 py-1 rounded-lg text-sm">950 pts</div>
    </div>
    <div class="flex items-center justify-between p-3 rounded-xl bg-gray-50">
      <div class="flex items-center gap-4">
        <span class="font-black text-lg w-6 text-gray-400">2</span>
        <div class="w-8 h-8 rounded-full bg-blue-100"></div>
        <span class="font-semibold text-gray-700">Anna Schmidt</span>
      </div>
      <div class="font-bold text-[#2B8EB5] bg-blue-50 px-3 py-1 rounded-lg text-sm">820 pts</div>
    </div>
  </div>

</div>