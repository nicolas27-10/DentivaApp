/**
 * Stripe client placeholder.
 * TODO: Install stripe and configure with secret key (server-side only).
 * TODO: Implement: subscription checkout, plan management, webhook handling.
 */

// import Stripe from 'stripe';

// const stripeSecretKey = import.meta.env.STRIPE_SECRET_KEY ?? '';

// export const stripe = new Stripe(stripeSecretKey, { apiVersion: '2024-11-20.acacia' });

export const stripe = null;
